import { ExperienceInformation } from './experience-information.model';

describe('ExperienceInformation', () => {
  it('should create an instance', () => {
    expect(new ExperienceInformation()).toBeTruthy();
  });
});
